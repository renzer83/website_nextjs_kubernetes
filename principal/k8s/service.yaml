apiVersion: v1
kind: Service
metadata:
  name: nexusai-principal
  labels:
    app: nexusai-principal
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: nexusai-principal
