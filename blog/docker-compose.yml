version: '3.8'

services:
  nexusai-blog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexusai-blog
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
