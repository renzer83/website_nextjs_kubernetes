apiVersion: v1
kind: Service
metadata:
  name: nexusai-features
  labels:
    app: nexusai-features
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: nexusai-features
