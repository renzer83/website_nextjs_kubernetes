apiVersion: v1
kind: Service
metadata:
  name: nexusai-contact
  labels:
    app: nexusai-contact
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: nexusai-contact
