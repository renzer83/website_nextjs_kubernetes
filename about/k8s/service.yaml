apiVersion: v1
kind: Service
metadata:
  name: nexusai-about
  labels:
    app: nexusai-about
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: nexusai-about
